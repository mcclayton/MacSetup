To update the `extensions.list` file, run the `updateExtensionList.sh` script.
The main `install.sh` script uses this list when installing VSCode packages.

`extensions.list` is a list of commonly used extensions that can each be
installed with `code --install-extension <extension>`.
