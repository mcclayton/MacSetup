# Vim

This directory contains the repo-managed Vim runtime files copied to `~/.vim`.

## Installed By

`setup_vim.sh` copies this directory to `~/.vim`, installs `~/.vimrc`, creates
`~/.vim/bundle`, and clones the configured Pathogen plugins automatically.

Plugin repositories are intentionally not vendored here.
